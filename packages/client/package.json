{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vite app. Renders the board and sends Orders — it never owns authoritative state.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "shared": "*"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
