# server

Placeholder. This package stays empty until **Stage B, Phase 9**, when it becomes
the router Worker plus a `GameRoom` Durable Object holding authoritative state.

It exists now only so the workspace layout matches the build plan and Stage B is
not a retrofit. Stage A ships entirely from `packages/client` as a static site —
no backend, no cost.
